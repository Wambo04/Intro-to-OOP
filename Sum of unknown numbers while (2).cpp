#include <iostream>

using namespace std;

int main()
{
    int i, x, y, sum=0;
    i=x;
    while (i<=y)
    {
        sum+=i;
        i++;
    }
    cout<<"total sum"<<sum;
    return 0;
}
